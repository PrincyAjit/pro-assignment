module.exports = {
  presets: ['@babel/preset-env', '@babel/preset-react'],
};

// presets to be used for transforming the JS code to code that is compatible with browser.
// preser-env is specifically for any ecma script features
// preset-react is for react code.
